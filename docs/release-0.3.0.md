# 灵动 0.3.0

修正发送路径：文字收拢 → 小球直达聊天区对应消息 → 展开，约 3.33 秒，不经过顶部。临时提交替换为正式消息时保持动画连续。

顶部任务球改为从 AI 回复区独立飞出，入场 2.2 秒。侧边栏改为整条会话按钮的彩色背景缓慢移动和交替，文字保持官方原色；当前会话与运行任务使用不同配色和节奏。

保留官方界面布局和控件。验证：8 项模型测试、3 组官方契约检查、38 项隔离浏览器场景及构建字节一致性通过。
