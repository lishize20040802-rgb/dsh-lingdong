# 0.5.4 验证

- 10 项状态/颜色/清理模型测试通过。
- 官方 0.1.7-rc.2 模块解析器、SlotCore 及实际气泡/输入结构 3 组核验通过。
- 隔离 Chromium 场景 44 项通过，无浏览器运行错误。
- 测试夹具包含官方气泡嵌套和独立复制操作区，覆盖真实文字快照、确认替换、长消息、目标布局变化、失败与会话切换。
- 轨迹采样核验最后一帧与真实目标矩形的偏差小于 4 px。
- 尚未在用户真实会话中发送消息验收；不会代用户发送测试内容。

- history never replays animations
- empty state has three adjacent solid balls on the native header background
- background jobs do not activate balls, count, subscriptions or sidebar activity
- one working agent boils in its own header position
- working ball stays a stable circle and only its small bubbles animate
- two working agents boil and orbit inside the header
- three working agents use intertwined trajectories
- orbs are solid macOS red yellow green with no glass layers
- upward bubbles share their parent solid color
- orbits actually move over time
- six agents preserve the first three identities and colors
- capsule exposes all six agent states
- Escape closes list and restores capsule focus
- sidebar glow is bound to the native row without extra children
- sidebar keeps its original fill under irregular blurred light patches
- native row and its glow move together when the sidebar reorders
- edge light spots flow instead of staying in a fixed frame
- native message actions remain visible during text flight
- shrinking text becomes a moving color ball
- host message stays accessible and visually held during flight
- optimistic replacement remains hidden until its copy arrives
- full text survives gathering without slicing
- all three continuous phases are rendered
- gather shrinks the entire text with scale instead of cropping
- expansion ends at the actual message bounds
- message restored after expansion
- gather uses real multiline input dimensions and font
- layout changes during flight restore the correct new target
- send failure immediately cancels and restores native message
- session switch cleans all transient visuals and leaves idle header balls
- one completed agent starts an in-place return while two keep working
- after the return, two remaining agents orbit
- completed balls arc back toward their original positions
- finished agents leave three calm balls in place
- completed agents settle back to three idle balls
- reduced motion separates three balls without animation
- reduced motion uses static send feedback
- narrow header has no horizontal overflow
- settings modal contains focus
- disable removes surface, row glow and orb effects
- native structured content retained
- unload cleans all styles, animations and row glow classes
- re-enable has exactly one style and capsule
- no browser runtime errors
