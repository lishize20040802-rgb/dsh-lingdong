# 验证报告

目标：官方 DeepSeek Harness Desktop **0.1.7-rc.2**；Windows；2026-09-25。

## 已完成

- esbuild 成功构建客户端 lazy-CJS 包；宿主 ESM 入口可导入。
- **7 项 Node 测试通过**：配置校验、官方状态优先级、未知进度、完成历史不重播、重新运行的子 agent、祖先会话活跃传播、客户端包协议与 React 共享。
- **3 组官方契约检查通过**：官方 bundle YAML 解析与 DSH 版本兼容性判断；官方 `parseDshClient` 及提供者包检查；真实官方 `SlotCore` 注册、卸载、再次启用。
- **20 项 Chromium 场景检查通过**：
  1. 历史消息首次挂载无入场动画。
  2. 四个子 agent 球与 `+N` 聚合。
  3. 聚焦会话标题沉静，超过三个后台项降低对比度。
  4. 键盘聚焦可见任务提示。
  5. 提交生成文字球且不修改消息原文。
  6. 飞行结束后清理浮层。
  7. 新自然语言段落与代码块触发不同动画。
  8. 后续流式追加不反复模糊已有文字。
  9. 发送失败取消动画。
  10. 快速连发只保留一个正在飞行的球。
  11. 切换会话隐藏原会话子 agent，并清理排队动画。
  12. 完成后所有球消散，包括聚合组中未显示的成员。
  13. 减少动态效果关闭冒泡。
  14. 减少动态效果下提交改用静态高亮。
  15. 窄桌面窗口无横向溢出。
  16. 设置关闭动效后清理所有增强。
  17. 高对比度下侧边栏文字仍可见。
  18. 卸载清理样式、观察器挂载区域、标题样式与飞行浮层。
  19. 再次启用只有一个样式表和顶部停靠区。
  20. 无浏览器运行时错误。

## 验证边界

Chromium 场景使用真实浏览器与 React，输入是仿照当前官方 DOM 和标准 hooks 的隔离测试夹具；不等于已在用户 Electron 实例中完成联调。官方契约检查使用本机同版本官方 npm 发布包，桌面应用版本从官方 app.asar 的 package.json 只读核对。

没有改写正在使用的 Desktop profile，没有向模型发送测试消息，没有访问真实聊天内容。安装后建议用一个新对话完成一次发送、流式回复、子 agent 和停用检查；若同时启用第三方界面替换插件，需要另行确认 DOM 兼容性。

原始文档内的全部 TBD 数值是本次实现选取的可调整默认值，不宣称用户曾确认这些具体参数。
